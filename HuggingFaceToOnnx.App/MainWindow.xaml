<Window x:Class="HuggingFaceToOnnx.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HuggingFaceToOnnx.App"
        xmlns:vm="clr-namespace:HuggingFaceToOnnx.App.ViewModels"
        mc:Ignorable="d"
        Title="HuggingFace to ONNX Converter" Height="500" Width="700">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Command Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Button Command="{Binding BrowseFilesCommand}" Content="Add Model Files..." Padding="10,5" />
        </Grid>

        <!-- Model List -->
        <GroupBox Grid.Row="1" Header="Models to Convert" Margin="0,0,0,10">
            <ListView ItemsSource="{Binding Models}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="File Path" DisplayMemberBinding="{Binding FilePath}" Width="400"/>
                        <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}" Width="100"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Output Directory -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Output Directory:" FontWeight="Bold"/>
            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding OutputDirectory}" Padding="5"/>
                <Button Command="{Binding BrowseOutputFolderCommand}" Content="..." Grid.Column="1" Width="30" Margin="5,0,0,0"/>
            </Grid>
        </StackPanel>

        <!-- Action -->
        <Button Grid.Row="3" 
                Command="{Binding ConvertCommand}" 
                Content="Convert All" 
                Height="30" Margin="0,0,0,10">
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Content" Value="Converting..."/>
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Logs -->
        <GroupBox Grid.Row="4" Header="Logs">
            <TextBox Text="{Binding LogOutput}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True"/>
        </GroupBox>

    </Grid>
</Window>
