<Window x:Class="HuggingFaceToOnnx.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HuggingFaceToOnnx.App"
        xmlns:vm="clr-namespace:HuggingFaceToOnnx.App.ViewModels"
        mc:Ignorable="d"
        Title="HuggingFace to ONNX Converter" Height="450" Width="600">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Model Selection -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Hugging Face Model ID:" FontWeight="Bold"/>
            <TextBox Text="{Binding ModelId, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" Padding="5"/>
        </StackPanel>

        <!-- Output Directory -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Output Directory:" FontWeight="Bold"/>
            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding OutputDirectory}" Padding="5"/>
                <Button Command="{Binding BrowseFolderCommand}" Content="..." Grid.Column="1" Width="30" Margin="5,0,0,0"/>
            </Grid>
        </StackPanel>

        <!-- Action -->
        <Button Grid.Row="2" 
                Command="{Binding ConvertCommand}" 
                Content="{Binding IsBusy, Converter={StaticResource BooleanToStringConverter}, FallbackValue='Convert'}" 
                Height="30" Margin="0,0,0,10">
            <Button.Style>
                <Style TargetType="Button">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsBusy}" Value="True">
                            <Setter Property="Content" Value="Converting..."/>
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <!-- Logs -->
        <GroupBox Grid.Row="3" Header="Logs">
            <TextBox Text="{Binding LogOutput}" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True"/>
        </GroupBox>

    </Grid>
</Window>
